<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: gitpress | Dingc Blog]]></title>
  <link href="https://qtdc1229.github.io/blog/categories/gitpress/atom.xml" rel="self"/>
  <link href="https://qtdc1229.github.io/"/>
  <updated>2014-10-11T15:41:37+08:00</updated>
  <id>https://qtdc1229.github.io/</id>
  <author>
    <name><![CDATA[dingc]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[利用Octopress写博客（在Mac环境搭建）]]></title>
    <link href="https://qtdc1229.github.io/blog/2014/10/09/li-yong-octopressxie-bo-ke-(zai-machuan-jing-da-jian-)/"/>
    <updated>2014-10-09T15:29:33+08:00</updated>
    <id>https://qtdc1229.github.io/blog/2014/10/09/li-yong-octopressxie-bo-ke-(zai-machuan-jing-da-jian-)</id>
    <content type="html"><![CDATA[<p><img src="http://beyondvincent.com/images/2013/08/github_page_and-octopress.png" alt="github + octopress" /></p>

<hr />

<h1>你好，gitpress</h1>

<p>大家好，这是我的第一篇blog，写些什么呢，就记录下怎么建立这个小blog的过程吧，权当大家参考。</p>

<h3>为什么搭建这个blog</h3>

<p>作为一个iOS开发者，互联网穿梭歌者，阅读各种文章必不可少，在这中间发现了<a href="http://beyondvincent.com/">破船之家</a>、<a href="http://onevcat.com/">OneV&rsquo;s Den</a>、<a href="http://blog.devtang.com/">唐巧的技术博客</a>这些优秀的iOS开发博客，在这当中发现这些博客几乎都是使用了Octopress这个framwork搭建的。Octopress本身是基于Jekyll的，而jekyll这个开源的blog软件是<a href="http://en.wikipedia.org/wiki/Tom_Preston-Werner">Tom Preston-Werner</a>用ruby完成的(wiki了一下这哥们原来是github的CEO，果然很NB)。什么样的blog软件值得这么多人风靡呢？基于这个疑问我只好hacker一把，尝试的完成了这个blog的搭建。</p>

<p>这个blog是搭建在github上的这个就不多说了，github已经成为coder界最神奇最风靡的一个异次元空间。Octopress因为基于jekyll所以对github支持非常优秀。搭建这个blog可以促进git的学习使用，同时markdown的书写习惯也可以多多练习。markdown编辑器是<a href="http://mouapp.com/">Mou</a>,书写方法参考了<a href="https://www.zybuluo.com/mdeditor">cmd在线编辑</a>。</p>

<h3>ruby + bundle + Octopress 的安装</h3>

<p>整个安装过程可以参考官方文档 <a href="http://octopress.org/docs/setup/">http://octopress.org/docs/setup/</a> 这里大概说说，自己遇到的问题也说一下。</p>

<p>mac系统本身是带有<code>ruby</code>的，这里可以确认下<code>ruby</code>的版本：</p>

<pre><code>    $ ruby --version //确认 ruby的版本
</code></pre>

<p>如果小于1.9.3或者没有安装需要先安装<code>RVM</code>，然后安装<code>ruby</code></p>

<pre><code>    $ curl -L https://get.rvm.io | bash -s stable --ruby ruby的版本
    $ rvm install 1.9.3
    $ rvm use 1.9.3
    $ rvm rubygems latest
</code></pre>

<p>然后再使用<code>ruby --version</code>确认<code>ruby</code>是否已经安装。
参考：<a href="http://octopress.org/docs/setup/rvm/">Installing Ruby With RVM</a></p>

<p>在安装Octopress之前，请确保你的电脑上已经安装有<code>git</code>了，在终端输入<code>git --version</code>，应该可以看到电脑中的<code>git</code>版本，如果没有显示相关内容，请先安装<code>git</code>。</p>

<p><code>git</code>安装之后，利用<code>git</code>命令将<code>octopress</code>从<code>github</code>上clone到本机</p>

<pre><code>    $ git clone git://github.com/imathis/octopress.git octopress
    $ cd octopress    # If you use RVM, You'll be asked if you trust the .rvmrc file (say yes).
</code></pre>

<p>接着安装相关依赖项：</p>

<pre><code>    $ gem install bundler
    $ rbenv rehash    # If you use rbenv, rehash to be able to run the bundle command
    $ bundle install
</code></pre>

<ul>
<li>这里使用<code>gem</code>的时候注意，由于<code>gem</code>默认的包是来源于Amazon的服务器所以很大的可能会被GFW阻挡，因此可以将<code>gem</code>的源转移到淘宝的<a href="https://ruby.taobao.org/">RubyGems</a>。</li>
</ul>


<p>最后安装默认的Octopress 主题。</p>

<pre><code>    $ rake install
</code></pre>

<p>参考： <a href="http://octopress.org/docs/setup/">Octopress Setup</a></p>

<h3>配置Octopress</h3>

<p>Octopress主要有三种部署模式：</p>

<ul>
<li>1、部署在GitHub Pages。<a href="http://octopress.org/docs/deploying/github">Deploying to GitHub Pages »</a></li>
<li>2、部署在Heroku。<a href="http://octopress.org/docs/deploying/heroku">Deploying to Heroku »</a></li>
<li>3、部署在自己的服务器。（Rsync模式）<a href="http://octopress.org/docs/deploying/rsync">Deploying with Rsync »</a></li>
</ul>


<p>这里主要说在github的部署，其余请查阅对应链接。</p>

<p>Octopress的作者已经尽量让配置简化了。大多数情况下只需要配置<code>_config.yml</code>和<code>Rakefile</code>文件即可。其中Rakefile是跟博客部署相关，一般情况下并不需要修改这个文件，除非使用了rsync。</p>

<p><code>config.yml</code>是博客重要的一个配置文件，在config.yml文件中有三大配置项：<code>Main Configs</code>、<code>Jekyll &amp; Plugins</code>和<code>3rd Party Settings</code>。</p>

<p>一般，该文件中其中<code>url</code>是必须要填写的，这里的url是在github上创建的一个仓库地址，具体请看第四步中创建的地址。另外再修改一下<code>title</code>、<code>subtitle</code>和<code>author</code>，根据需求，在开启一些第三方组件服务。</p>

<p>关于<code>_config.yml</code>文件中的更多内容，请看这里的内容：<a href="http://octopress.org/docs/configuring/">Configuring Octopress</a></p>

<h3>将Octopress部署到GitHub Pages上</h3>

<p>首先需要在github上申请一个免费账号，然后<a href="https://github.com/new">创建一个资源库</a>资源库需要命名为：<code>username.github.io</code>、<code>username.github.com</code>或<code>organization.github.com</code>，这个地址就是配置完毕后我们访问blog的地址。
创建好仓库后我们需要利用octopress在rakefile文件中配置好的的task来设置blog。可以先使用下面的命令：</p>

<pre><code>    $ rake setup_github_pages
</code></pre>

<p>上面的命令会做一些事情(详细介绍看下面给出的参考链接)。其中最主要的就是创建一个_deploy目录，目录用来存放部署到master分支的内容。期间会要求你输入仓库的url，根据提示，进行输入即可。</p>

<p>完成上面的命令后我们就可以生成blog，然后部署到服务器上了。</p>

<pre><code>    $ rake generate
    $ rake deploy
</code></pre>

<p>执行玩<code>deploy</code>后，就可以访问刚才你申请资源库名称了。</p>

<p>至此你已经基本完成了octopress的部署。</p>

<h3>新添加一篇blog</h3>

<p>Octopress为我们提供了一些task来创建博文和页面。博文必须存储在source/_posts目录下，并且需要按照Jekyll的命名规范对文章进行命名：YYYY-MM-DD-post-title.markdown。文章的名字会被当做url的一部分，而其中的日期用于对博文的区分和排序。</p>

<p>通过Octopress提供的task可以正确的按照命名规范创建一个博文，并且在博文中会附带常用的一些yaml元数据。只需要在终端输入如下命令：</p>

<pre><code>    $ rake new_post["title"]
</code></pre>

<p>其中title为博文的文件名，创建出来的文件默认是markdown格式。上面的命令会创建出这样一个文件：<code>source/_posts/2014-10-09-title.markdown</code>。</p>

<p>打开这个文件，可以看到里面有如下一些内容了(告诉Jekyll博客引擎如何处理博文和页面)：</p>

<pre><code>    ---
    layout: post
    title: "title"
    date: 2013-08-03 16:36
    comments: true
    categories: 
    ---
</code></pre>

<p>接着我们就可以在这个文件中写我们的博文啦。完成之后，我们可以预览和部署博文。下面是创建并部署博文的一个完整过程：</p>

<pre><code>    $ rake new_post["New Post"]
    $ rake generate
    $ git add .
    $ git commit -am "Some comment here." 
    $ git push origin source
    $ rake deploy
</code></pre>

<p>参考：<a href="http://octopress.org/docs/blogging/">Start blogging with Octopress</a></p>

<h4>小结</h4>

<p>在这之后还需要做很多，比如评论插件、分享插件的添加，有自己域名了可以把这个再指向自己的域名，还有一些关于自己个性化的配置。在之后慢慢完善吧。</p>

<h4>参考</h4>

<ul>
<li>1、<a href="http://beyondvincent.com/blog/2013/08/03/108-creating-a-github-blog-using-octopress/">利用Octopress搭建一个Github博客</a></li>
<li>2、<a href="http://blog.devtang.com/blog/2012/02/10/setup-blog-based-on-github/">象写程序一样写博客：搭建基于github的博客</a></li>
<li>3、<a href="http://octopress.org/docs/">Octopress Documentation</a></li>
</ul>

]]></content>
  </entry>
  
</feed>
